{% assign current_variant = product.selected_or_first_available_variant %}
{% assign featured_image = current_variant.featured_image | default: product.featured_image %}
<img src="{{ featured_image | img_url: 'large' }}" alt="{{ featured_image.alt | escape }}" id="ProductPhotoImg">
{% for image in product.images %}
  <a href="{{ image.src | img_url: 'large' }}">
    <img src="{{ image.src | img_url: 'compact' }}" alt="{{ image.alt | escape }}">
  </a>
{% endfor %}
<h1>{{ product.title }}</h1>
<form action="/cart/add" method="post" enctype="multipart/form-data" id="AddToCartForm">
  <select name="id" id="productSelect">
    {% for variant in product.variants %}
      {% if variant.available %}
        <option value="{{ variant.id }}">
          {{ variant.title }} - {{ variant.price | money_with_currency }}
        </option>
      {% else %}
        <option disabled="disabled">
          {{ variant.title }} - sold out
        </option>
      {% endif %}
<div class="max-w-4xl mx-auto">
  <img class="w-full rounded-lg mb-4" src="{{ featured_image | img_url: 'large' }}" alt="{{ featured_image.alt | escape }}" id="ProductPhotoImg">
  <div class="flex space-x-2 mb-4">
    {% for image in product.images %}
      <a href="{{ image.src | img_url: 'large' }}">
        <img class="w-20 h-20 object-cover rounded-md" src="{{ image.src | img_url: 'compact' }}" alt="{{ image.alt | escape }}">
      </a>
    {% endfor %}
  </select>
  {{ current_variant.price | money }}
  <label for="Quantity">quantity</label>
  <input type="number" id="Quantity" name="quantity" value="1" min="1">
  <button type="submit" name="add" id="AddToCart">Add to cart</button>
</form>
<div>{{ product.description }}</div>
  </div>
  <h1 class="text-3xl font-bold mb-2">{{ product.title }}</h1>
  <form action="/cart/add" method="post" enctype="multipart/form-data" id="AddToCartForm" class="mb-4 space-y-2">
    <select name="id" id="productSelect" class="border rounded p-2 w-full">
      {% for variant in product.variants %}
        {% if variant.available %}
          <option value="{{ variant.id }}">
            {{ variant.title }} - {{ variant.price | money_with_currency }}
          </option>
        {% else %}
          <option disabled="disabled">
            {{ variant.title }} - sold out
          </option>
        {% endif %}
      {% endfor %}
    </select>
    <div class="text-xl font-semibold">{{ current_variant.price | money }}</div>
    <label for="Quantity" class="block">Quantity</label>
    <input type="number" id="Quantity" name="quantity" value="1" min="1" class="border rounded p-2 w-20">
    <button type="submit" name="add" id="AddToCart" class="btn">
      {% render 'ic-shopping-bag' %}
      <span class="ml-1">Add to cart</span>
    </button>
  </form>
  <div class="prose max-w-none">{{ product.description }}</div>
</div>
